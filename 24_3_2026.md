# POS - Výpisky
24 March 2026

---

## Synchronizace

Týká se procesů nebo vláken, která **sdílí paměť**.

Musí být splněno:
- **Konkurentní systém** s **preemptivním multitaskingem**
- Na úrovni **userspace**

Týká se **kritického kódu** → vede ke **kolizím** a kvůli špatné synchronizaci můžeme dostat **špatné výsledky**.

---

### Klasický příklad: Producent & Konzument

- **Producent** – zapisuje/přepisuje sdílenou paměť
- **Konzument** – očekává, že přečte celou (platnou) paměť

```
PRODUCENT → [sdílená paměť] → KONZUMENT
```

---

### Problémy se synchronizací

1. **Konzument běží první** – přečte nezapsanou (neplatnou) paměť
   → Producent musí běžet jako první (časová závislost)

2. **Producent zapíše 2×, konzument přečte jen 1×**
   → Ztráta dat (např. chybějící měření ze senzoru)

3. **Producent zapíše 1×, konzument přečte N×**
   → Duplicitní údaje (stejná data zpracována vícekrát)

---

### Vzájemné vyloučení (Mutual Exclusion) ⚠️ DŮLEŽITÝ TERMÍN

> **V jednom okamžiku může ke sdílené paměti přistupovat pouze jeden proces.**

- Procesy/vlákna nemohou přistupovat ke sdílené paměti současně
- Složitější scénáře: **více producentů a více konzumentů** → vzájemné vyloučení stále platí

```
Proces A  →|  kritická sekce  |→ ...
Proces B     (čeká, nemůže vstoupit současně)
```

> ⚠️ Vzájemné vyloučení **není největší cíl** synchronizace – mnoho lidí si to mylně myslí.
> Hlavní cíl je **správné pořadí**: producent musí běžet jako první.

---

### Co dál – jak synchronizace dosáhnout?

Probrali jsme problém producenta a konzumenta. Nyní se zaměříme na **mechanismy**, jak správné synchronizace dosáhnout.

---

### Synchronizační nástroje / Synchronizační primitiva

- Existují jako **SW i HW** řešení
- Slouží k zajištění vzájemného vyloučení nebo správného pořadí
- Dosáhneme toho právě pomocí těchto primitiv

---

### Binární semafor

> Nejstarší synchronizační primitivum – vzniklo na konci **50. let / začátku 60. let**.

- **Dvoustavový** – pouze dvě hodnoty:
  - 🔴 červená (zamčeno / obsazeno)
  - 🟢 zelená (volno / přístup povolen)
- ≠ semafor na křižovatce (jiný koncept)

#### Příklad: Vlaky a koleje

Klasická analogie pro vzájemné vyloučení:

```
        čeká                   jede
         |                      |
[🚂]====🔴====+================+====🟢====[🚂]>>
              |  jednokolejný  |
              |     úsek       |
              | (kritická sekce)|
              +================+
```

- Jednokolejný úsek = **kritická sekce** (sdílený zdroj)
- Semafor zajistí, že úsekem jede vždy **jen jeden vlak**
- Druhý vlak čeká, dokud úsek není volný

→ Přesná analogie vzájemného vyloučení v OS
→ Semafor na křižovatce je daleko komplexnější (chodci, více směrů...) – tato jednokolejná trať je ideální zjednodušení

---

#### Dvě operace nad semaforem

Různá jména dle literatury, Fišer používá:

| Operace | Alternativní názvy | Popis |
|---------|-------------------|-------|
| **WAIT** | acquire, down, P | Vstup do kritické sekce |
| **SIGNAL** | release, up, V | Výstup z kritické sekce |

#### WAIT – příjezd k semaforu

```
           🔴 ČERVENÁ → zastav a čekej na změnu stavu
vlak →→→ [?]
           🟢 ZELENÁ  → projeď a nastav semafor na 🔴
```

#### SIGNAL – odjezd od semaforu

```
vlak opouští úsek → nastav semafor na 🟢
```

#### Implementace WAIT v pseudokódu – naivní (ŠPATNĚ ❌)

```python
def wait(semafor):
    if semafor == RED:
        block(change)    # zastav a čekej na změnu stavu
    if semafor == GREEN:
        semafor = RED    # nastav červenou a vstup do kritické sekce
```

> ⚠️ **Toto nefunguje správně!**
> Mohlo by čekat více lokomotiv – po změně na 🟢 by **všechny** viděly zelenou a projely zároveň.
> Pokud bychom u `if` zůstali, musela by být implementována **čekací fronta**.

#### Implementace WAIT v pseudokódu – správně ✅

```python
def wait(semafor):
    while semafor == RED:  # po probuzení znovu zkontroluj stav
        block(change)      # zastav a čekej na změnu stavu
    semafor = RED          # nastav červenou a vstup do kritické sekce
```

> S `while` si každý probuzený proces sám znovu ověří stav → jen jeden projde.

#### Implementace v praxi

- V **userspace** je preemptivní multitasking složitý → správná implementace semaforu je náročná
- Proto se semafor implementuje v **jádře OS** s kooperativním přístupem
- S **jedním procesorem** to v jádře jde relativně snadno

#### Vícejádrové procesory – problém

- Více jader (např. 96jádrový procesor) → jádra spolu musí komunikovat a synchronizovat se navzájem
- **Zamykání sběrnice** = ostatní nesmí přistupovat ke sběrnici, i když přistupují k něčemu jinému → **zpomaluje celý systém**
- Řešení: **speciální HW podpora**
  - Zámek se aktivuje pouze tehdy, když **další procesor chce provést tu samou operaci**
  - Jinak ostatní jádra pokračují bez čekání
  - Operace musí být **atomická a rychlá**
  - Na základě této HW podpory jsme schopni implementovat semafor správně

> ⚠️ **DŮLEŽITÉ PRAVIDLO:**
> **Nikdy nepsat semafor běžnými operacemi!**
> Vždy použít buď:
> - **Jádro OS** (které poskytuje synchronizační primitiva) – např. mutexy, semafory
> - Pokud OS primitiva **neposkytuje** → použít **atomické instrukce** (HW podpora)

#### SIGNAL – implementace

- Jednoduchá operace – pouze nastav semafor na 🟢 zelená
- Žádná podmínka, žádné čekání

```python
def signal(semafor):
    semafor = GREEN    # nastav zelenou – kritická sekce je volná
```

#### Celý průběh

```
1. Vlak A přijede:  WAIT  → 🟢 zelená → vjede, nastaví 🔴
2. Vlak B přijede:  WAIT  → 🔴 červená → čeká
3. Vlak A odjíždí:  SIGNAL → nastaví 🟢
4. Vlak B pokračuje: WAIT  → 🟢 zelená → vjede, nastaví 🔴
```

---

### Použití semaforu – Producent & Konzument

> Semafory je lepší vytvářet v **zeleném stavu** (ne červeném).

#### Pouze vzájemné vyloučení (1 semafor) – nestačí ⚠️

```python
# jeden sdílený semafor (začíná GREEN)
while True:
    wait(s)       # zamkni
    write(data)   # zapiš do sdílené paměti
    signal(s)     # odemkni

# konzument
while True:
    wait(s)       # zamkni
    read(data)    # čti ze sdílené paměti
    signal(s)     # odemkni
```

> Problém: producent může zapsat 2× a konzument se nemusí dostat ke čtení → **pouze vzájemné vyloučení, ale ne časová synchronizace**.

#### Časová synchronizace (2 semafory) – správně ✅

Každý čeká na **svém** semaforu, ale signalizuje ten **opačný**.

```
Počáteční stav:
  sem_producent = 🟢  (producent může hned psát)
  sem_konzument = 🔴  (konzument čeká, až producent zapíše)
```

```python
# producent
while True:
    wait(sem_producent)   # čekej než smím psát
    write(data)           # zapiš
    signal(sem_konzument) # dej vědět konzumentovi

# konzument
while True:
    wait(sem_konzument)   # čekej než smím číst
    read(data)            # přečti
    signal(sem_producent) # dej vědět producentovi
```

```
  sem_producent=🟢          sem_konzument=🔴
       |                          |
       ▼                          ▼
  [PRODUCENT]                [KONZUMENT]
  wait(sem_P) ──🟢──►  write(data)
                              │
                         signal(sem_K) ──► sem_konzument=🟢
                                                │
                                           [KONZUMENT]
                                           wait(sem_K) ──🟢──► read(data)
                                                                    │
                                                               signal(sem_P) ──► sem_producent=🟢
                                                                                      │
                                                                                 (opakuje se)
```

#### Symetrie kódu – záleží jen na pohledu

Kód producenta a konzumenta vypadá **symetricky** – bez kontextu nevíme, kdo je kdo.
Záleží pouze na tom, **jak situaci chápeme**:

| Pohled | Producent | Konzument |
|--------|-----------|-----------|
| Klasický | zapisuje data | čte/maže data |
| Duální | produkuje plný prostor | produkuje **volný** prostor |

> Konzument tím, že přečte/smaže data, **produkuje volný prostor** pro producenta.
> Producent tím, že zapíše, **produkuje plný prostor** pro konzumenta.
>
> Kód je tedy symetrický – jde jen o to, **jak ho pojmenujeme a jak mu rozumíme**.

#### Příklad: Hostinský a hosté 🍺

- **Hostinský** plní půllitry pivem → produkuje plné půllitry
- **Hosté** pijí a vracejí prázdné půllitry → produkují prázdné půllitry

Nebo duálně:
- Hosté produkují **prázdné půllitry** a hostinský je konzumuje (tím že je plní, přestávají být prázdné)

> V realitě to ale **symetrické není** – intuitivně víme, kdo je producent a kdo konzument.
> Symetrie je jen matematická vlastnost kódu, ne reálného problému.

---

### Nevýhody binárního semaforu – proč se dnes nepoužívá

#### Deadlock (uváznutí)

> Pokud zavoláme `wait()` **dvakrát** nad stejným semaforem → **DEADLOCK**

- Proces čeká sám na sebe, aby semafor uvolnil
- Nekonečná závislost → program **zmrzne**
- Deadlock může vzniknout vždy, i omylem (zapomeneme zavolat `signal`, voláme `wait` 2×, ...)
- Poznáme ho tak, že **program přestane reagovat**

```
wait(s)   ← zamkne
  ...
wait(s)   ← čeká na uvolnění... ale kdo ho uvolní? Nikdo → DEADLOCK ∞
```

---

### Nadstavba nad semafory – Monitor / synchronized

Nad semafory existují **komplexnější prostředky** – např. **synchronizační monitor**.

#### Java – klíčové slovo `synchronized`

```java
synchronized void metoda() {
    // vzájemné vyloučení zajištěno automaticky
    // při vstupu se zamkne, při opuštění se uvolní
}
```

- Při volání `synchronized` metody se **automaticky zamkne** objekt
- Při opuštění se **automaticky uvolní** → nelze zapomenout zavolat `signal`
- Proto je to **bezpečnější než semafor**

#### Ale pozor – deadlock i u monitoru! ⚠️

```java
synchronized void metodaA() {
    metodaB();   // volá jinou synchronized metodu stejného objektu
}

synchronized void metodaB() {
    // pokus o zamknutí stejného zámku → DEADLOCK
}
```

- Pokud uvnitř `synchronized` metody zavoláme **jinou `synchronized` metodu** se stejným zámkem → **deadlock**
- Nejčastější příčina: **vnořená volání** metod stejného objektu
- Buď složitá logika, nebo to prostě **nebude fungovat**

#### Další nevýhoda – víceúrovňové systémy

- Více producentů a konzumentů (např. 8 producentů, 8 konzumentů)
- Musíme správně volat `wait`/`signal` pro každého → **velmi náchylné na chyby**
- Pro složitější úlohy jsou semafory **těžkopádné a nepraktické**

---

### Moderní synchronizační primitiva

Místo binárního semaforu se dnes používají dvě specializovaná primitiva:

| Primitivum | Účel |
|------------|------|
| **Mutex** | Vzájemné vyloučení |
| **Synchronizační událost (Event)** | Časová synchronizace (správné pořadí) |

- **Mutex** – zajistí, že ke kritické sekci přistupuje vždy jen jeden proces/vlákno
- **Event** – zajistí, že jeden proces počká, dokud druhý nedokončí svou práci (producent → konzument)

---

### Mutex

Podobá se semaforu, ale je **chytřejší**.

#### Klíčový rozdíl oproti semaforu

| | Semafor | Mutex |
|--|---------|-------|
| Stav | 0 / 1 (červená / zelená) | kdo ho vlastní + kolikrát |
| Vlastnictví | neví, kdo ho zamkl | **pamatuje si, kdo ho zamkl** |
| Operace | wait / signal | **lock / unlock** (nebo acquire / release) |

> **Mutex je vlastněn** – ví, který proces/vlákno ho zamklo.
> Semafor to neví – je anonymní (jen 0/1).

#### Jak funguje

- Uchovává **identitu vlastníka** a **počet zamknutí**
- Pokud stejné vlákno zavolá `lock` znovu → neuvázne (ví, že je to stejný vlastník)
- Uvolnit může **pouze ten, kdo ho zamkl**

#### Logika lock()

```
příjdu k mutexu
    │
    ├── vlastní ho někdo jiný → ČEKÁM
    │
    ├── nikdo ho nevlastní    → PŘIVLASTNÍM SI HO (vlastník = já, počet = 1)
    │
    └── vlastním ho já        → ZAMKNU ZNOVU (počet += 1)
```

```python
def lock(mutex):
    if mutex.owner == jiný:
        wait()                  # čekej
    elif mutex.owner == nikdo:
        mutex.owner = já        # přivlastni
        mutex.count = 1
    elif mutex.owner == já:
        mutex.count += 1        # jen zvyš počet (reentrantní)

def unlock(mutex):
    mutex.count -= 1
    if mutex.count == 0:
        mutex.owner = nikdo     # úplně uvolni
```

> Je složitější než semafor, ale **lépe se používá** a předchází mnoha chybám.

#### Důležitá pravidla mutexu

- **Odemknout může POUZE ten, kdo ho zamkl** – nikdo jiný
- Mutex slouží **výhradně pro vzájemné vyloučení** – nic víc
- Když potřebujeme chránit přístup ke sdílenému zdroji → použijeme **mutex**
- V Pythonu se mutex často označuje jako **Lock** (`threading.Lock`)

---

### Synchronizační událost (Event)

Nejjednodušší synchronizační primitivum – je **bezestavová**.

| Operace | Chování |
|---------|---------|
| **wait()** | Vždy se zablokuje – nelze se ptát na stav, protože žádný není |
| **notify()** | Probudí **všechny** zablokované nad touto událostí |

> Netestujeme žádný stav – prostě čekáme nebo oznamujeme.

---

### Producent & Konzument v praxi – kompletní řešení

Pro správné nahrazení semaforu potřebujeme **3 věci**:

| # | Co | K čemu |
|---|----|--------|
| 1 | **Proměnná** (stav) | Nese informaci – plné / prázdné |
| 2 | **Mutex** | Zajistí vzájemné vyloučení při přístupu k proměnné |
| 3 | **Event** | Signalizuje změnu stavu proměnné |

```python
M      = Mutex()   # vzájemné vyloučení
CHANGE = Event()   # signalizace změny stavu
stav   = EMPTY     # počáteční stav – paměť je prázdná

# EMPTY = prázdná paměť
# FULL  = plná paměť

# počáteční stav: empty = True (paměť je prázdná)

# počáteční stav: empty = True (paměť je prázdná)

# --- producent ---
while True:
    M.lock()
    if not empty:       # paměť je plná → čekej na změnu
        M.unlock()      # uvolni mutex před čekáním!
        CHANGE.wait()
        M.lock()        # znovu zamkni po probuzení
    write(mem)          # zapiš do sdílené paměti
    CHANGE.notify()     # oznam konzumentovi – stav se změnil
    M.unlock()

# --- konzument ---
while True:
    M.lock()
    if empty:           # paměť je prázdná → čekej na změnu
        M.unlock()      # uvolni mutex před čekáním!
        CHANGE.wait()
        M.lock()        # znovu zamkni po probuzení
    read(mem)           # čti ze sdílené paměti
    CHANGE.notify()     # oznam producentovi – stav se změnil
    M.unlock()

# Jeden CHANGE event stačí pro oba směry.
# Kdyby byly 2 eventy, každá strana by signalizovala ten opačný.

> ❌ **Náhodný deadlock (verze 2) – chyba je u `if`!**
>
> Problém: mezi `M.unlock()` a `CHANGE.wait()` může nastat přepnutí kontextu:
>
> ```
> 1. Producent: M.lock()
> 2. Producent: paměť plná → M.unlock()
>    --- CONTEXT SWITCH ---
> 3. Konzument: M.lock() → přečte → CHANGE.notify()  ← notify se ztratí, nikdo nečeká!
> 4. Konzument: M.unlock()
>    --- zpět na producenta ---
> 5. Producent: CHANGE.wait()  ← čeká NAVŽDY, notify už proběhl
>    → DEADLOCK (ztracené probuzení)
> ```
>
> `M.unlock()` a `CHANGE.wait()` **nejsou atomické** → notify může přijít dřív než wait.
>
> Přerušení kdekoli jinde nevadí – **problém nastane POUZE tehdy, když přijde context switch uvnitř toho `if` bloku**, mezi `M.unlock()` a `CHANGE.wait()`.
>
> ### ⚠️ Tento jev se nazývá: EVENT LEAK
>
> Událost (notify) se „ztratí" – proběhne dřív než někdo čeká → nikdo ji nezachytí.
>
> **Nelze vyřešit na úrovni tohoto kódu.** Musí to řešit nižší vrstva.

---

### Úrovně synchronizace

| Úroveň | Název | Výhody | Nevýhody |
|--------|-------|--------|----------|
| 1 | **Syncless** (bez synchronizace) | ✅ rychlost, žádná režie | ❌ nekonzistentní, nedeterministický stav |
| 2 | **Podsynchronizace** | ✅ většinou funguje (99,99 %) | ❌ volný prostor pro kolize, **špatně se ladí!** |
| 3 | **Optimální synchronizace** | ✅ konzistentní, deterministický, minimální režie | ❌ těžké na implementaci |
| 4 | **Přesynchronizace** | ✅ konzistentní | ❌ zbytečné čekání, zbytečná režie, pomalé |

> - **Podsynchronizace** je nejnebezpečnější – chyba se může projevit v kritický okamžik a špatně se debuguje
> - **Optimální** synchronizace je ideál – minimální prostředky, žádné zbytečné čekání
> - **Přesynchronizace** je v praxi **běžný stav** – raději pomalejší než rozbité
>
> Speciální případ přesynchronizace: **Deadlock** – přesynchronizace tak extrémní, že je to **matematicky nekonečné čekání** → program nikdy nepokračuje

#### Předcházení deadlocku – více zdrojů najednou

- Máme **5 zdrojů** a proces potřebuje získat mutexy pro více z nich
- **Špatné řešení:** zamknu co mám a čekám na zbytek → kruhová závislost → DEADLOCK
- **Správné řešení:** pokud se mi nepodaří získat všechny potřebné prostředky → **uvolním i ty co držím** a zkusím znovu

```
zkus zamknout mutex 2
  ├── úspěch → pokračuj
  └── neúspěch → uvolni mutex 1 (i to co už držím) → zkus celé znovu
```

---

### Bariéra (Barrier)

Velmi častý synchronizační vzor – teprve když dorazí **všech N procesů**, mohou všechny pokračovat.

Potřebujeme:
- **Mutex** – chrání sdílený čítač
- **Proměnná** (čítač) – kolik procesů už dorazilo
- **Event** – signalizuje dosažení N

```python
M       = Mutex()
READY   = Event()
counter = 0       # počet procesů, které dorazily k bariéře

def barrier(N):
    M.lock()
    counter += 1          # přičti svůj příchod
    if counter < N:
        READY.wait(M)     # ještě nejsou všichni → čekej (atomicky uvolní M)
    else:
        READY.notify()    # poslední proces → probuď všechny
        M.unlock()
```

```
Proces 1 →→→ barrier() → counter=1 → čeká
Proces 2 →→→ barrier() → counter=2 → čeká
Proces 3 →→→ barrier() → counter=3 == N → notify() → všichni pokračují
```

---

### Read-Write zámky (vylepšené mutexy)

#### Problém s klasickým mutexem

- Mutex neumožní číst 2 procesům najednou
- Ale čtení **nemění stav** → žádná kolize při souběžném čtení → zbytečná přesynchronizace

#### Řešení – Read-Write zámek (2 stavy)

| Stav | Typ zámku | Kdo ho může držet | K čemu |
|------|-----------|-------------------|--------|
| **Sdílený** (shared) | Read lock | Více procesů najednou | Čtení |
| **Exkluzivní** (exclusive) | Write lock | Pouze jeden proces | Zápis |

```
Čtení:  Proces A ──┐
                    ├── sdílený zámek → mohou číst oba najednou ✅
        Proces B ──┘

Zápis:  Proces A ── exkluzivní zámek → nikdo jiný nesmí číst ani psát ✅
```

> Čtení = sdílený zámek (shared) → více procesů současně
> Zápis = exkluzivní zámek (exclusive) → pouze jeden, nikdo další

---

### Klasický vznik deadlocku – 2 mutexy, 2 procesy

```python
# P1 – zamyká v pořadí M1 → M2
M1.lock()
M2.lock()
# ... práce ...
M2.unlock()
M1.unlock()

# P2 – zamyká v OBRÁCENÉM pořadí M2 → M1
M2.lock()
M1.lock()
# ... práce ...
M1.unlock()
M2.unlock()
```

#### Jak deadlock vznikne:

```
P1: M1.lock()  → drží M1
                     --- context switch ---
P2: M2.lock()  → drží M2
P2: M1.lock()  → čeká (M1 drží P1)
                     --- zpět na P1 ---
P1: M2.lock()  → čeká (M2 drží P2)

→ P1 čeká na P2, P2 čeká na P1 → DEADLOCK ∞
```

> Příčina: procesy zamykají stejné mutexy v **opačném pořadí**
> Řešení: vždy zamykat mutexy ve **stejném pořadí** napříč všemi procesy

#### Nevýhoda tohoto řešení

Stejné pořadí zamykání **nemusí být vždy přirozené**.

Příklad:
- Potřebuji zároveň získat **socket** a čekat na **proces**
- Co mám zamknout dřív? Socket nebo proces?
- Pořadí není vždy logicky zřejmé → **může snadno vzniknout chyba v pořadí zamykání**

---

#### Správné řešení – atomický wait s mutexem

`CHANGE.wait()` dostane jako parametr **mutex** → speciální služba jádra OS, která:
1. **Atomicky odemkne mutex** a zároveň
2. **Vstoupí do čekání** na notifikaci

```python
M.unlock()        # ← toto už neděláme ručně
CHANGE.wait()     # ← toto už neděláme ručně

# správně:
CHANGE.wait(M)    # atomicky: odemkni M + čekej na notify (neoddělitelné!)
```

> Tato operace **musí být napsána v jádře OS** a musí být správně implementována.
> Pouze jádro dokáže zajistit, že unlock + wait proběhnou bez možnosti context switche mezi nimi.
>
> Jde o jednu komplexní metodu, která pracuje nad **2 synchronizačními objekty** (mutex + event) a dělá **3 věci najednou** – atomicky:
> 1. Odemkne mutex
> 2. Začne čekat na event
> 3. Po probuzení znovu zamkne mutex

> ❌ **Proč tohle nebude fungovat? → DEADLOCK (verze 1)**
>
> 1. Konzument získá `M.lock()`
> 2. Paměť je prázdná → zavolá `CHANGE.wait()` → zablokuje se
> 3. Konzument **stále drží mutex M** !
> 4. Producent zkusí `M.lock()` → **zablokuje se** (mutex drží konzument)
> 5. Producent nemůže zapsat ani zavolat `CHANGE.notify()`
> 6. Konzument čeká navždy → **DEADLOCK**
>
> Stejný problém nastane i obráceně (producent drží mutex, čeká na CHANGE, konzument se nedostane k notify).
>
> **Řešení:** `CHANGE.wait()` musí **atomicky uvolnit mutex** při čekání a znovu ho získat po probuzení.
> → Toto řeší **Condition variable** (v Pythonu `threading.Condition`)
```


